# Core notebook environment
jupyterlab>=4.0
ipywidgets>=8.0

# Display / plotting (pure visualisation, no canonical math)
numpy>=1.25
matplotlib>=3.7

# RQM ecosystem — rqm-notebooks sits on top of rqm-qiskit.
# qiskit and qiskit-aer are intentionally omitted here: they arrive as
# transitive dependencies of rqm-qiskit.  All circuit construction and
# execution must go through the rqm-qiskit abstraction layer.
rqm-core>=0.1.0
rqm-qiskit>=0.1.0

# Development / testing
pytest>=7.4
nbformat>=5.9
nbconvert>=7.0
