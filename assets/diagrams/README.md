# assets/diagrams

Source files for architecture and concept diagrams used across rqm-notebooks.

Store editable diagram sources here (e.g. `.drawio`, `.mermaid`, `.svg`).
Exported PNG/SVG for use in notebooks go in `../images/`.
